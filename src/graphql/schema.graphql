type User {
  id: String!
  name: String!
}

type Query {
  getUsers: [User]
}

type Mutation {
  createUser(name: String!): User!
  room(userName: String!): [User]
}

type Subscription {
  user: User!
}
